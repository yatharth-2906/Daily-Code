#include<stdio.h>

int main(void)
{
    int x =2, y= 3;
    printf("The value of 3*x/2*y is: %d \n",3*x/2*y);
    printf("The value of (3*x)/(2*y) is: %d \n",(3*x)/(2*y));
    printf("The value of 2*y/3*x is: %d \n",2*y/3*x);
    printf("The value of (2*y)/(3*x) is: %d",(2*y)/(3*x));
}
